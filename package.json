{
  "name": "sat-scraper",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "postinstall": "playwright install chromium --with-deps || playwright install chromium"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.48.0",
    "node-forge": "^1.3.1"
  },
  "engines": {
    "node": ">=20"
  }
}
